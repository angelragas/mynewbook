# https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html#id2
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt


conda:
  environment: environment.yml

sphinx:
  configuration: _build/html/conf.py